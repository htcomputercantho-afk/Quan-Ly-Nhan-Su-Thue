<Window x:Class="TaxPersonnelManagement.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Language="vi-VN"
        Title="Quản Lý Nhân Sự" Height="700" Width="1000"
        WindowStartupLocation="CenterScreen"
        Icon="icon.ico"
        WindowState="Maximized"
        FontFamily="{DynamicResource MaterialDesignFont}"
        FontSize="15"
        SizeChanged="MainWindow_SizeChanged">
    
    <Grid>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition x:Name="colSidebar" Width="250"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

        <!-- Sidebar (Deep Blue Brand Color) -->
        <Border Grid.Column="0" Grid.RowSpan="2" Background="{DynamicResource PrimaryColor}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Hidden" HorizontalScrollBarVisibility="Disabled">
                    <StackPanel>
                        <StackPanel.Resources>
                             <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                                <Setter Property="Height" Value="50"/>
                                <Setter Property="HorizontalContentAlignment" Value="Left"/>
                                <Setter Property="Padding" Value="25 0"/>
                                <Setter Property="Margin" Value="10 5"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="materialDesign:ButtonAssist.CornerRadius" Value="10"/>
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#33FFFFFF"/> <!-- Semi-transparent white hover -->
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </StackPanel.Resources>
                        
                        <Button x:Name="btnLogoToggle" Click="btnToggleSidebar_Click" 
                                Background="Transparent" BorderThickness="0" Height="70" Margin="0 10" 
                                HorizontalAlignment="Stretch" HorizontalContentAlignment="Center" Padding="0"
                                Cursor="Hand" ToolTip="Thu gọn/Phóng to menu"
                                Style="{StaticResource {x:Type Button}}">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <Image x:Name="imgLogo" Source="/icon.ico" Width="36" Height="36" VerticalAlignment="Center" RenderOptions.BitmapScalingMode="HighQuality" Margin="0 0 10 0"/>
                                <TextBlock x:Name="txtLogo" Text="QUẢN LÝ NHÂN SỰ" Foreground="White" FontSize="16" FontWeight="Bold" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
        
                        <Separator Background="White" Opacity="0.3" Margin="20 10 20 20"/>
        
                        <!-- Navigation Buttons (White on Blue) -->
                        <Button x:Name="btnDashboard" Click="NavigateDashboard" ToolTip="Tổng quan">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon x:Name="iconDashboard" Kind="ViewDashboard" Width="22" Height="22" Margin="0 0 15 0"/>
                                <TextBlock x:Name="txtOverview" Text="Tổng quan" FontSize="15" FontWeight="SemiBold"/>
                            </StackPanel>
                        </Button>
        
                        <Button x:Name="btnPersonnel" Click="NavigatePersonnel" ToolTip="Nhân sự">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon x:Name="iconPersonnel" Kind="AccountGroup" Width="22" Height="22" Margin="0 0 15 0"/>
                                <TextBlock x:Name="txtPersonnel" Text="Nhân sự" FontSize="15" FontWeight="SemiBold"/>
                            </StackPanel>
                        </Button>
        
                        <Button x:Name="btnSalary" Click="NavigateSalary" ToolTip="Nâng lương">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon x:Name="iconSalary" Kind="CashMultiple" Width="22" Height="22" Margin="0 0 15 0"/>
                                <TextBlock x:Name="txtSalary" Text="Nâng lương" FontSize="15" FontWeight="SemiBold"/>
                            </StackPanel>
                        </Button>
        
                        <Button x:Name="btnAnnualIncome" Click="NavigateAnnualIncome" ToolTip="Tổng thu nhập năm">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon x:Name="iconAnnualIncome" Kind="ChartLine" Width="22" Height="22" Margin="0 0 15 0"/>
                                <TextBlock x:Name="txtAnnualIncome" Text="Tổng thu nhập năm" FontSize="15" FontWeight="SemiBold"/>
                            </StackPanel>
                        </Button>
        
                        <Button x:Name="btnLeaveDetail" Click="NavigateLeaveDetail" ToolTip="Chi tiết nghỉ phép">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="CalendarClock" Width="22" Height="22" VerticalAlignment="Center" Margin="0 0 15 0"/>
                                <TextBlock x:Name="txtLeaveDetail" Text="Chi tiết phép năm" VerticalAlignment="Center" FontSize="14" FontWeight="Medium"/>
                            </StackPanel>
                        </Button>

                        <Button x:Name="btnPositionDuration" Click="NavigatePositionDuration" ToolTip="Thời gian giữ vị trí công tác">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="Timer" Width="22" Height="22" Margin="0 0 15 0" VerticalAlignment="Center"/>
                                <TextBlock x:Name="txtPositionDuration" Text="TG giữ vị trí công tác" FontSize="14" FontWeight="Medium" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
        
                        <Button x:Name="btnEmulationReward" Click="NavigateEmulationReward" ToolTip="Thi đua khen thưởng">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon x:Name="iconEmulationReward" Kind="Medal" Width="22" Height="22" Margin="0 0 15 0" VerticalAlignment="Center"/>
                                <TextBlock x:Name="txtEmulationReward" Text="Thi đua khen thưởng" FontSize="14" FontWeight="Medium" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>

                        <Separator Background="White" Margin="20" Opacity="0.3"/>
                        
                        <Button x:Name="btnUsers" Click="NavigateUsers" ToolTip="Tài khoản">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon x:Name="iconUsers" Kind="AccountKey" Width="22" Height="22" Margin="0 0 15 0"/>
                                <TextBlock x:Name="txtUsers" Text="Tài khoản" FontSize="15" FontWeight="SemiBold"/>
                            </StackPanel>
                        </Button>
        
                        <Button x:Name="btnBackupRestore" Click="NavigateBackupRestore" ToolTip="Sao lưu &amp; Phục hồi">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon x:Name="iconBackupRestore" Kind="DatabaseCog" Width="22" Height="22" Margin="0 0 15 0"/>
                                <TextBlock x:Name="txtBackupRestore" Text="Sao lưu &amp; Phục hồi" FontSize="15" FontWeight="SemiBold"/>
                            </StackPanel>
                        </Button>
                        
                        <Button x:Name="btnLogout" Click="Logout_Click" Margin="10 20" ToolTip="Đăng xuất">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon x:Name="iconLogout" Kind="Logout" Width="22" Height="22" Margin="0 0 15 0"/>
                                <TextBlock x:Name="txtLogout" Text="Đăng xuất" FontSize="15" FontWeight="SemiBold"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </ScrollViewer>
                <StackPanel Grid.Row="1" VerticalAlignment="Center">
                    <TextBlock x:Name="txtCopyright" Text="Copyright © 2026 Linhoang" Foreground="White" Opacity="0.6" FontSize="13" FontFamily="Segoe UI" FontWeight="Medium" HorizontalAlignment="Center" Margin="0 5 0 0"/>
                    <TextBlock x:Name="txtVersion" Text="Version 1.0.0.0" Foreground="White" Opacity="0.4" FontSize="11" HorizontalAlignment="Center" Margin="0 2 0 15"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Column="1" Background="#F5F5F5">
            <Grid.RowDefinitions>
                <RowDefinition Height="60"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" Background="White" Effect="{DynamicResource MaterialDesignShadowDepth1}">
                <Grid Margin="10 0">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center">
                        <TextBlock Text="QUẢN LÝ NHÂN SỰ" FontWeight="Bold" FontSize="15" Foreground="{DynamicResource PrimaryHueMidBrush}" Margin="15 0 0 0" VerticalAlignment="Center"/>
                    </StackPanel>

                    <!-- Account Details (Right side of Header) -->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                        <TextBlock Text="Xin chào, " VerticalAlignment="Center" Foreground="Gray"/>
                        <TextBlock x:Name="txtWelcome" Text="User" VerticalAlignment="Center" FontWeight="Bold" Foreground="{DynamicResource PrimaryHueMidBrush}" Margin="5 0 0 0"/>
                        <materialDesign:PackIcon Kind="AccountCircle" Width="30" Height="30" Foreground="{DynamicResource PrimaryHueMidBrush}" Margin="10 0 0 0" VerticalAlignment="Center"/>
                    </StackPanel>
                </Grid>
            </Border>

            <Grid Grid.Row="1" Margin="12 10">
                <Frame x:Name="MainFrame" NavigationUIVisibility="Hidden"/>
            </Grid>
        </Grid>
        </Grid>
    </Grid>
</Window>
