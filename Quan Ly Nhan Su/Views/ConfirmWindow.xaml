<Window x:Class="TaxPersonnelManagement.Views.ConfirmWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        SizeToContent="Height" Width="400"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">
    
    <Border Background="White" CornerRadius="15" BorderBrush="#1976D2" BorderThickness="1" Margin="20">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="5" Opacity="0.2"/>
        </Border.Effect>
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" VerticalAlignment="Center" HorizontalAlignment="Center">
                <materialDesign:PackIcon Kind="HelpCircle" Width="50" Height="50" Foreground="#1976D2" HorizontalAlignment="Center" Margin="0 0 0 15"/>
                <TextBlock x:Name="txtTitle" Text="Xác Nhận" FontSize="20" FontWeight="Bold" Foreground="#333" HorizontalAlignment="Center" Margin="0 0 0 10"/>
                <TextBlock x:Name="txtMessage" Text="Bạn có chắc chắn muốn thực hiện?" FontSize="14" Foreground="#555" TextWrapping="Wrap" TextAlignment="Center" HorizontalAlignment="Center"/>
            </StackPanel>
            
            <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0 20 0 0">
                <Button Content="Đồng ý" Click="Yes_Click" Margin="0 0 10 0" 
                        Width="100" Height="35"
                        Background="#1976D2" BorderBrush="#1976D2" Foreground="White"
                        ToolTip="Xác nhận thực hiện"/>
                
                <Button Content="Hủy bỏ" Click="No_Click" 
                        Width="100" Height="35"
                        Background="Transparent"
                        BorderBrush="#757575" BorderThickness="1" Foreground="#757575"
                        ToolTip="Đóng cửa sổ"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
