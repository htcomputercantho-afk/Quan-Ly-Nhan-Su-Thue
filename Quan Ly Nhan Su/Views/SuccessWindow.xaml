<Window x:Class="TaxPersonnelManagement.Views.SuccessWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Height="180" Width="300"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">
    
    <Border Background="White" CornerRadius="15" BorderBrush="#E0E0E0" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="5" Opacity="0.2"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" VerticalAlignment="Center" HorizontalAlignment="Center">
                <materialDesign:PackIcon Kind="CheckCircle" Width="60" Height="60" Foreground="#4CAF50" HorizontalAlignment="Center" Margin="0 0 0 15"/>
                <TextBlock x:Name="txtMessage" Text="Cập nhật thành công!" FontSize="18" FontWeight="Bold" Foreground="#333" HorizontalAlignment="Center"/>
            </StackPanel>
            
            <Button Grid.Row="1" Content="Đóng" Click="Close_Click" Margin="0 0 0 20" 
                    Width="100" Height="35"
                    Style="{StaticResource PremiumButtonStyle}" 
                    HorizontalAlignment="Center"/>
        </Grid>
    </Border>
</Window>
