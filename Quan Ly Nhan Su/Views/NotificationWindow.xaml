<Window x:Class="TaxPersonnelManagement.Views.NotificationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        SizeToContent="Height" Width="450"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">
    
    <Border Background="White" CornerRadius="15" BorderBrush="#2196F3" BorderThickness="1" Margin="20">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="5" Opacity="0.2"/>
        </Border.Effect>
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" VerticalAlignment="Center" HorizontalAlignment="Center">
                <materialDesign:PackIcon Kind="Information" Width="50" Height="50" Foreground="#2196F3" HorizontalAlignment="Center" Margin="0 0 0 15"/>
                <TextBlock x:Name="txtTitle" Text="Thông báo" FontSize="20" FontWeight="Bold" Foreground="#333" HorizontalAlignment="Center" Margin="0 0 0 10"/>
                <TextBlock x:Name="txtMessage" Text="Nội dung thông báo..." FontSize="15" Foreground="#555" TextWrapping="Wrap" TextAlignment="Center" HorizontalAlignment="Center" LineHeight="24"/>
            </StackPanel>
            
            <Button Grid.Row="1" Content="Đóng" Click="Close_Click" Margin="0 20 0 0" 
                    Width="100" Height="35"
                    Style="{DynamicResource MaterialDesignRaisedButton}"
                    materialDesign:ButtonAssist.CornerRadius="10"
                    Background="#2196F3" BorderBrush="#2196F3" Foreground="White"
                    HorizontalAlignment="Center"/>
        </Grid>
    </Border>
</Window>
