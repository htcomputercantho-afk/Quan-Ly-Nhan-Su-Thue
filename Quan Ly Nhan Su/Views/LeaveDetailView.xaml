<Page x:Class="TaxPersonnelManagement.Views.LeaveDetailView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d" 
      d:DesignHeight="700" d:DesignWidth="1000"
      Title="Chi tiết phép năm"
      xmlns:converters="clr-namespace:TaxPersonnelManagement.Converters">
    
    <Page.Resources>
        <converters:Base64ToImageConverter x:Key="Base64ToImageConverter"/>
    </Page.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <Grid Background="#F5F5F5" Margin="15">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/> <!-- Header & Search -->
                <RowDefinition Height="*"/>    <!-- DataGrid Container -->
            </Grid.RowDefinitions>

            <!-- Top Header & Controls -->
            <Border Grid.Row="0" Background="White" CornerRadius="8" Padding="20" Margin="0 0 0 15" Effect="{DynamicResource MaterialDesignShadowDepth1}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock x:Name="txtHeaderTitle" Grid.Row="0" Grid.Column="0" 
                               Text="BẢNG TỔNG HỢP SỐ NGÀY NGHỈ PHÉP THỰC TẾ NĂM" 
                               FontWeight="Bold" FontSize="20" VerticalAlignment="Center" 
                               Foreground="{DynamicResource PrimaryHueMidBrush}"/>



                        <Button x:Name="btnExportExcel" Grid.Column="1" Height="36" Click="BtnExportExcel_Click" 
                                Style="{DynamicResource MaterialDesignRaisedButton}"
                                Background="#1976D2" BorderBrush="#1976D2" Foreground="White" Padding="15,0"
                                materialDesign:ButtonAssist.CornerRadius="18" HorizontalAlignment="Right">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="FileExcel" Width="18" Height="18" Margin="0 0 8 0" VerticalAlignment="Center" Foreground="White"/>
                                <TextBlock Text="Xuất báo cáo Excel" FontWeight="Medium" VerticalAlignment="Center" FontSize="14"/>
                            </StackPanel>
                        </Button>

                    <!-- Modern Search Bar -->
                    <Border Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Margin="0 20 0 5" 
                            Background="White" BorderBrush="#E0E0E0" BorderThickness="1.5" 
                            CornerRadius="22" Padding="15 2" HorizontalAlignment="Left" Width="600"
                            Effect="{DynamicResource MaterialDesignShadowDepth1}">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="BorderBrush" Value="{DynamicResource PrimaryHueMidBrush}"/>
                                        <Setter Property="Background" Value="#FAFAFA"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <materialDesign:PackIcon Kind="Magnify" Grid.Column="0" Width="24" Height="24" 
                                                    Foreground="{DynamicResource PrimaryHueMidBrush}" 
                                                    VerticalAlignment="Center" Margin="0 0 12 0"/>
                            
                            <TextBox x:Name="txtSearch" Grid.Column="1" FontSize="15"
                                     materialDesign:HintAssist.Hint="Tìm kiếm theo Tên, Mã số cán bộ hoặc CCCD..." 
                                     materialDesign:TextFieldAssist.DecorationVisibility="Collapsed"
                                     BorderThickness="0" Background="Transparent" VerticalAlignment="Center"
                                     TextChanged="TxtSearch_TextChanged"
                                     CaretBrush="{DynamicResource PrimaryHueMidBrush}"/>

                            <Button x:Name="btnClearSearch" Grid.Column="2" Width="30" Height="30" 
                                    Style="{DynamicResource MaterialDesignToolButton}" 
                                    Background="Transparent" BorderThickness="0"
                                    Padding="0" Margin="0 0 5 0" Visibility="Collapsed"
                                    Click="BtnClearSearch_Click">
                                <materialDesign:PackIcon Kind="CloseCircle" Width="18" Height="18" Foreground="#9E9E9E"/>
                            </Button>
                        </Grid>
                    </Border>
                </Grid>
            </Border>

            <!-- Main DataGrid Container -->
            <Border Grid.Row="1" Background="White" CornerRadius="10" Padding="0" 
                    BorderBrush="#E0E0E0" BorderThickness="1"
                    Effect="{DynamicResource MaterialDesignShadowDepth2}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                <DataGrid x:Name="dgLeaveSummary" Grid.Row="0" AutoGenerateColumns="False" CanUserAddRows="False"
                          IsReadOnly="True" HeadersVisibility="Column" SelectionMode="Single"
                          BorderThickness="0" Background="White"
                          RowHeight="NaN" MinRowHeight="50" ColumnWidth="*"
                          GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#EEEEEE"
                          AlternatingRowBackground="#F8FAFD" RowBackground="White"
                          ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                          ScrollViewer.VerticalScrollBarVisibility="Auto"
                          FontSize="14">

                    <!-- Header Style: Nền xanh đậm, chữ trắng -->
                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource {x:Type DataGridColumnHeader}}">
                            <Setter Property="HorizontalContentAlignment" Value="Center"/>
                            <Setter Property="Padding" Value="10,8"/>
                            <Setter Property="MinHeight" Value="75"/>
                            <Setter Property="Background" Value="#1565C0"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="FontSize" Value="13"/>
                            <Setter Property="BorderBrush" Value="#1256A8"/>
                            <Setter Property="BorderThickness" Value="0,0,1,0"/>
                            <Setter Property="ContentTemplate">
                                <Setter.Value>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding}" TextWrapping="Wrap" TextAlignment="Center" 
                                                   Foreground="White" FontWeight="Bold" FontSize="13.5"
                                                   LineHeight="20"/>
                                    </DataTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </DataGrid.ColumnHeaderStyle>

                    <!-- Row Style: Hover effect -->
                    <DataGrid.RowStyle>
                        <Style TargetType="DataGridRow">
                            <Setter Property="Margin" Value="0"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#E3F2FD"/>
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="#BBDEFB"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.RowStyle>

                    <!-- Cell Style -->
                    <DataGrid.CellStyle>
                        <Style TargetType="DataGridCell">
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Padding" Value="8,6"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="Foreground" Value="Black"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.CellStyle>

                    <DataGrid.Columns>
                        <!-- STT -->
                        <DataGridTextColumn Header="STT" Binding="{Binding STT}" Width="60">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="HorizontalAlignment" Value="Center"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                    <Setter Property="FontSize" Value="14"/>
                                    <Setter Property="Foreground" Value="#616161"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <!-- Mã số cán bộ - REMOVED BY USER REQUEST -->

                        <!-- Avatar + Họ và tên -->
                        <DataGridTemplateColumn Header="Họ và tên" Width="2.2*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="12,10">
                                        <Grid Width="50" Height="50" Margin="0,0,15,0">
                                            <!-- Fallback Icon Layer -->
                                            <Ellipse Fill="#E3F2FD" Stroke="#BBDEFB" StrokeThickness="1"/>
                                            <materialDesign:PackIcon Kind="AccountCircle" Width="36" Height="36" 
                                                                     Foreground="#1976D2" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            
                                            <!-- Actual Avatar Image Layer -->
                                            <Ellipse Stroke="#E0E0E0" StrokeThickness="1">
                                                <Ellipse.Fill>
                                                    <ImageBrush ImageSource="{Binding AvatarBase64, Converter={StaticResource Base64ToImageConverter}}" 
                                                                Stretch="UniformToFill"
                                                                RenderOptions.BitmapScalingMode="HighQuality"/>
                                                </Ellipse.Fill>
                                            </Ellipse>
                                        </Grid>
                                        <TextBlock Text="{Binding FullName}" VerticalAlignment="Center" 
                                                   FontWeight="Bold" FontSize="16" Foreground="#212121"
                                                   TextWrapping="Wrap" MaxWidth="180"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- Số CCCD -->
                        <DataGridTextColumn Header="Số CCCD" Binding="{Binding IdentityCardNumber}" Width="1.4*">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="HorizontalAlignment" Value="Center"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="FontSize" Value="14"/>
                                    <Setter Property="Foreground" Value="#424242"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <!-- Phép tiêu chuẩn -->
                        <DataGridTemplateColumn Header="Số ngày được nghỉ phép" Width="1.1*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border Background="#E8F5E9" CornerRadius="8" Padding="10,6" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="4">
                                        <TextBlock Text="{Binding TotalTarget}" FontWeight="Bold" FontSize="16" 
                                                   Foreground="#2E7D32" HorizontalAlignment="Center"/>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- Phép năm cũ (n-1) -->
                        <DataGridTemplateColumn x:Name="colOldYear" Header="Nghỉ phép năm cũ" Width="1.1*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border Background="#F3E5F5" CornerRadius="8" Padding="10,6" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="4">
                                        <TextBlock Text="{Binding TakenFromOldYear}" FontWeight="Bold" FontSize="16" 
                                                   Foreground="#7B1FA2" HorizontalAlignment="Center"/>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- Phép năm nay (n) -->
                        <DataGridTemplateColumn x:Name="colCurrentYear" Header="Nghỉ phép năm nay" Width="1.1*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border Background="#E1F5FE" CornerRadius="8" Padding="10,6" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="4">
                                        <TextBlock Text="{Binding TakenFromCurrentYear}" FontWeight="Bold" FontSize="16" 
                                                   Foreground="#0288D1" HorizontalAlignment="Center"/>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- Thực tế đã nghỉ (Tổng) -->
                        <DataGridTemplateColumn Header="Tổng" Width="1.1*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border Background="#FFF3E0" CornerRadius="8" Padding="10,6" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="4">
                                        <TextBlock Text="{Binding ActualTaken}" FontWeight="Bold" FontSize="16" 
                                                   Foreground="#E65100" HorizontalAlignment="Center"/>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- Nội dung chi tiết -->
                        <DataGridTemplateColumn Header="Nội dung chi tiết" Width="3.5*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <ItemsControl ItemsSource="{Binding DetailLines}" Margin="15,10">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <TextBlock TextWrapping="Wrap" LineHeight="24" FontSize="16" Foreground="#424242">
                                                    <Run Text="{Binding MainContent, Mode=OneWay}"/>
                                                    <Run Text="{Binding Suffix, Mode=OneWay}" FontWeight="Bold" Foreground="#D32F2F"/>
                                                </TextBlock>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- Số phép còn lại -->
                        <DataGridTemplateColumn Header="Phép còn lại" Width="1.2*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border Background="#E8F5E9" CornerRadius="10" Padding="12,8" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="4">
                                        <TextBlock Text="{Binding Remaining}" FontWeight="Bold" FontSize="16" 
                                                   Foreground="#1B5E20" HorizontalAlignment="Center"/>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>

                    <!-- Pagination Footer -->
                    <Grid Grid.Row="1" Margin="15 8">
                        <TextBlock x:Name="txtPagingInfo" Text="Hiển thị 0 nhân viên" VerticalAlignment="Center" Foreground="#78909C" FontSize="12"/>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button x:Name="btnPrevPage" Click="BtnPrevPage_Click" 
                                    Style="{DynamicResource MaterialDesignIconForegroundButton}" Width="30" Height="30" Margin="0 0 5 0">
                                <materialDesign:PackIcon Kind="ChevronLeft" Foreground="#607D8B"/>
                            </Button>
                            <TextBlock x:Name="txtPageInfo" Text="1 / 1" VerticalAlignment="Center" Margin="10 0" FontWeight="Bold" Foreground="#455A64"/>
                            <Button x:Name="btnNextPage" Click="BtnNextPage_Click" 
                                    Style="{DynamicResource MaterialDesignIconForegroundButton}" Width="30" Height="30">
                                <materialDesign:PackIcon Kind="ChevronRight" Foreground="#607D8B"/>
                            </Button>
                        </StackPanel>
                    </Grid>
                </Grid>
            </Border>
        </Grid>
    </ScrollViewer>
</Page>
