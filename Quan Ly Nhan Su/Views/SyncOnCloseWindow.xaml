<Window x:Class="TaxPersonnelManagement.Views.SyncOnCloseWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Height="220" Width="400"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True">

    <Border Background="White" CornerRadius="16" Margin="15">
        <Border.Effect>
            <DropShadowEffect BlurRadius="30" ShadowDepth="4" Opacity="0.25" Color="#1565C0"/>
        </Border.Effect>
        <Grid Margin="20">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <!-- Spinning Cloud Icon -->
                <materialDesign:PackIcon Kind="CloudSync" Width="50" Height="50" Foreground="#1565C0" HorizontalAlignment="Center" Margin="0 0 0 12" RenderTransformOrigin="0.5,0.5">
                    <materialDesign:PackIcon.RenderTransform>
                        <RotateTransform x:Name="rotateTransform" Angle="0"/>
                    </materialDesign:PackIcon.RenderTransform>
                    <materialDesign:PackIcon.Triggers>
                        <EventTrigger RoutedEvent="FrameworkElement.Loaded">
                            <BeginStoryboard>
                                <Storyboard RepeatBehavior="Forever">
                                    <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(RotateTransform.Angle)" From="0" To="360" Duration="0:0:1.5"/>
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                    </materialDesign:PackIcon.Triggers>
                </materialDesign:PackIcon>

                <TextBlock Text="Đang lưu CSDL lên Google Drive..." FontSize="16" FontWeight="Bold" Foreground="#1565C0" HorizontalAlignment="Center" Margin="0 0 0 6"/>
                <TextBlock Text="Vui lòng chờ trong giây lát trước khi đóng..." FontSize="13" Foreground="#78909C" HorizontalAlignment="Center"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
