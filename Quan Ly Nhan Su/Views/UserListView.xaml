<UserControl x:Class="TaxPersonnelManagement.Views.UserListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:converters="clr-namespace:TaxPersonnelManagement.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <converters:RoleToDisplayNameConverter x:Key="RoleConverter"/>
    </UserControl.Resources>
    <materialDesign:Card Margin="20" Style="{DynamicResource MaterialDesignCard}">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Quản lý tài khoản" FontSize="20" FontWeight="Bold" Margin="0 0 0 20"/>

            <!-- Toolbar -->
            <Grid Grid.Row="1" Margin="0 0 0 15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" MinWidth="200" MaxWidth="400"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Search Box -->
                <Grid Grid.Column="0">
                    <TextBox x:Name="txtSearch" 
                             materialDesign:HintAssist.Hint="Tìm kiếm tài khoản, họ tên..."
                             Padding="30 8 10 8" TextChanged="TxtSearch_TextChanged"
                             BorderBrush="#B0BEC5" BorderThickness="1"
                             materialDesign:TextFieldAssist.HasClearButton="True"
                             VerticalAlignment="Center">
                    </TextBox>
                    <materialDesign:PackIcon Kind="Magnify" Width="20" Height="20" 
                                             Foreground="Gray" 
                                             VerticalAlignment="Center" HorizontalAlignment="Left" Margin="8 0 0 0" IsHitTestVisible="False"/>
                </Grid>

                <!-- Add Button -->
                <Button Grid.Column="2" 
                        Background="#1976D2"
                        BorderBrush="#1976D2"
                        Foreground="White"
                        ToolTip="Thêm tài khoản mới"
                        Width="150"
                        Height="36"
                        Click="BtnAddUser_Click"
                        materialDesign:ButtonAssist.CornerRadius="18">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Add" Width="20" Height="20" Margin="0 0 5 0"/>
                        <TextBlock Text="Thêm tài khoản" VerticalAlignment="Center" FontWeight="Medium"/>
                    </StackPanel>
                </Button>
            </Grid>

            <!-- DataGrid -->
            <DataGrid Grid.Row="2" x:Name="dgUsers" AutoGenerateColumns="False" IsReadOnly="True"
                      CanUserAddRows="False" CanUserDeleteRows="False"
                      SelectionMode="Single" HeadersVisibility="Column"
                      BorderThickness="1" BorderBrush="#E0E0E0"
                      RowHeight="45" Background="White" AlternatingRowBackground="#FAFAFA"
                      GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#EEEEEE"
                      VerticalGridLinesBrush="Transparent"
                      ScrollViewer.HorizontalScrollBarVisibility="Auto"
                      ScrollViewer.VerticalScrollBarVisibility="Auto">
                <DataGrid.Resources>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#1976D2"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="FontSize" Value="14"/>
                        <Setter Property="Padding" Value="10 5"/>
                        <Setter Property="HorizontalContentAlignment" Value="Center"/>
                        <Setter Property="VerticalContentAlignment" Value="Center"/>
                        <Setter Property="BorderThickness" Value="0"/>
                    </Style>
                    <Style TargetType="DataGridCell">
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="DataGridCell">
                                    <Border Padding="{TemplateBinding Padding}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" SnapsToDevicePixels="True">
                                        <ContentPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                    <Style x:Key="CenterAlignedCell" TargetType="TextBlock">
                        <Setter Property="HorizontalAlignment" Value="Center"/>
                        <Setter Property="VerticalAlignment" Value="Center"/>
                    </Style>
                    <Style x:Key="LeftAlignedCell" TargetType="TextBlock">
                        <Setter Property="HorizontalAlignment" Value="Left"/>
                        <Setter Property="VerticalAlignment" Value="Center"/>
                        <Setter Property="Margin" Value="15 0"/>
                    </Style>
                    <!-- Button Style for Actions -->
                    <Style x:Key="RowActionButton" TargetType="Button">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Width" Value="32"/>
                        <Setter Property="Height" Value="32"/>
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="Margin" Value="4 0"/>
                        <Setter Property="Cursor" Value="Hand"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" CornerRadius="16">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#E0E0E0"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </DataGrid.Resources>

                <DataGrid.Columns>
                    <DataGridTextColumn Binding="{Binding Id}" Header="ID" Width="60" ElementStyle="{StaticResource CenterAlignedCell}"/>
                    <DataGridTextColumn Binding="{Binding Username}" Header="Tên đăng nhập" Width="*" MinWidth="150" ElementStyle="{StaticResource LeftAlignedCell}"/>
                    <DataGridTextColumn Binding="{Binding FullName}" Header="Họ tên" Width="1.5*" MinWidth="200" ElementStyle="{StaticResource LeftAlignedCell}"/>
                    <DataGridTextColumn Binding="{Binding Role, Converter={StaticResource RoleConverter}}" Header="Quyền hạn" Width="150" ElementStyle="{StaticResource CenterAlignedCell}"/>
                    
                    <DataGridTemplateColumn Header="Hành động" Width="160">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <Button x:Name="btnEditUser" Click="BtnEditUser_Click" Tag="{Binding Id}"
                                            Style="{StaticResource RowActionButton}" ToolTip="Sửa thông tin" Foreground="#1976D2">
                                        <materialDesign:PackIcon Kind="Pencil" Width="18" Height="18" />
                                    </Button>
                                    <Button x:Name="btnChangePassword" Click="BtnChangePassword_Click" Tag="{Binding Id}"
                                            Style="{StaticResource RowActionButton}" ToolTip="Đổi mật khẩu" Foreground="#388E3C">
                                        <materialDesign:PackIcon Kind="KeyVariant" Width="18" Height="18" />
                                    </Button>
                                    <Button x:Name="btnDeleteUser" Click="BtnDeleteUser_Click" Tag="{Binding Id}"
                                            Style="{StaticResource RowActionButton}" ToolTip="Xóa tài khoản" Foreground="#E53935">
                                        <materialDesign:PackIcon Kind="Delete" Width="18" Height="18" />
                                    </Button>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </materialDesign:Card>
</UserControl>
